apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: intensive-load-test
  namespace: prod
spec:
  mode: all
  selector:
    labelSelectors:
      app: web-frontend
  stressors:
    cpu:
      workers: 2
      load: 92
  duration: "10m"